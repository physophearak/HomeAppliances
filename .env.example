# Paste your deployed Google Apps Script web app URL here.
# Leave empty to run in offline demo mode with local sample data.
VITE_GAS_URL=

# Optional: override the USD -> KHR exchange rate (default 4100).
VITE_KHR_RATE=4100
